# Copy to .env and adjust.

# Base domain served by the external Traefik proxy. The app is published at
# https://baby-utils.$DOMAIN, the optional sync API at https://baby-utils.$DOMAIN/api/sync.
DOMAIN=example.com
